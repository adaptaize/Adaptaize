<?xml version="1.0" encoding="UTF-8"?>
<svg width="240" height="48" viewBox="0 0 240 48" fill="none" xmlns="http://www.w3.org/2000/svg">
    <!-- Main Logo Container -->
    <g transform="translate(0, 4)">
        <!-- Logo Icon -->
        <g transform="translate(0, 0)">
            <!-- Outer Circle -->
            <circle cx="24" cy="20" r="20" fill="#6B46C1" opacity="0.1" />
            <!-- Inner Circle -->
            <circle cx="24" cy="20" r="16" fill="#6B46C1" />
            <!-- AI Letters -->
            <text x="24" y="26" font-family="Arial" font-size="16" font-weight="900" fill="white"
                text-anchor="middle">AI</text>
        </g>

        <!-- Brand Name -->
        <g transform="translate(52, 0)">
            <!-- Adapt -->
            <text x="0" y="26" font-family="Arial" font-size="24" font-weight="900" fill="#6B46C1"
                letter-spacing="0.5">Adapt</text>
            <!-- AI -->
            <text x="85" y="26" font-family="Arial" font-size="24" font-weight="900" fill="#553C9A"
                letter-spacing="0.5">AI</text>
            <!-- ze -->
            <text x="115" y="26" font-family="Arial" font-size="24" font-weight="900" fill="#553C9A"
                letter-spacing="0.5">ze</text>
        </g>
    </g>

    <!-- Decorative Elements -->
    <path d="M220 8 L230 8 L225 16 Z" fill="#6B46C1" opacity="0.2" />
    <path d="M225 40 L235 40 L230 32 Z" fill="#553C9A" opacity="0.2" />
</svg>